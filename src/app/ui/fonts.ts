import { Kalam, Frijole } from 'next/font/google';

export const kalam = Kalam({
  subsets: ['latin'],
  weight: ['300', '400', '700'],
});

export const frijole = Frijole({
  subsets: ['latin'],
  weight: '400'
});